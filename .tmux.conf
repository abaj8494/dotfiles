set -g prefix C-a

unbind-key C-b
bind-key C-a send-prefix

bind -r Tab select-pane -t :.+

set -g mouse on

# vim-like pane switching (theprimeagen)
bind -r k select-pane -U
bind -r j select-pane -D
bind -r h select-pane -L
bind -r l select-pane -R

# testing dotfiles git
